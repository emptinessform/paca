module github.com/paca/plugin-sdk

go 1.24

// This module has no third-party dependencies; it is intentionally minimal so
// that plugin authors can depend on it without importing the full API service
// dependency tree.
